import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  fingerPrint: {
    alignItems: "center",
    marginTop: 40,
  },
  fingerPrintTitle: {
    fontSize: 20,
    color: "#5B5B5E",
    marginBottom: 20,
  },
});

export default styles;
