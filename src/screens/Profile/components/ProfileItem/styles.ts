import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        alignItems: 'center',
        marginBottom: 15
    },
    icon: {
        padding: 20,
        borderColor: '#DEDEDE',
        borderWidth: 1,
        borderRadius: 14,
    },
    desciption: {
        fontSize: 20,
        fontWeight: '600',
        color: '#5B5B5E',
        marginLeft: 15
    },
});

export default styles;