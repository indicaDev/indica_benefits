import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {},
    text: {
        fontSize: 24,
        fontWeight: '600',
        color: '#9796A1',
        textAlign: 'center',
    },
    image: {
        alignSelf: 'center',
        marginTop: 70
    }
});

export default styles;